# Backend base URL the dashboard talks to. Read by src/api/client.ts.
# In dev, either point at the running Django server (uses the CORS setup)
# or leave unset to fall back to "/api/v1" and rely on the Vite proxy
# configured in vite.config.ts.
VITE_API_BASE_URL=http://localhost:8000/api/v1